import React from 'react';
import {arrowDownIcon, askQuestionIcon, bellIcon, calendarIcon, logoIcon} from './../assets/index';


const TestComponent: React.FC<any> = () => {

    return (
        <div className='flex'>
        </div>
    );
}

export default TestComponent;